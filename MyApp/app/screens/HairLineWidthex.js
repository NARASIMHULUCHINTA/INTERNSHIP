import React, { Component } from 'react';
import HairLineWidth from '../components/HairLineWidth/HairLineWidth';
export class HairLineWidthex extends Component {
    render() {
        return (
            <HairLineWidth />
        )
    }
}

export default HairLineWidthex